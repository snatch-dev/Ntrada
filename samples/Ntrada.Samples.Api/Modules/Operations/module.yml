name: Operations
path: /operations

routes:
- upstream: /{id}
  method: GET
  use: downstream
  downstream: operations-service/operations/{id}
  auth: false
  
services:
  operations-service:
    local_url: localhost:5004
    url: load_balancer/operations-service